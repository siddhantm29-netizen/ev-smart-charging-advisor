# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# Environment / secrets
.env

# Data — regenerate locally via `python src/data_fetch.py`, don't commit large files
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Models — regenerate locally via `python src/forecast.py`, don't commit large files
models/

# OS / editor
.DS_Store
.vscode/