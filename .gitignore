# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# Environment / secrets
.env

# Models — figures/CSVs/metrics are tracked (small, human-readable); the
# serialized model weights are regenerable via `python src/forecast.py` and
# large enough (~3MB each) to skip
models/xgboost_*.json
models/prophet_*.json

# OS / editor
.DS_Store
.vscode/